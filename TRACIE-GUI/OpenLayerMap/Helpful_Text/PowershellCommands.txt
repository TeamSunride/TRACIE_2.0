cd OpenLayerMap


cd TRACIE_COPY/TRACIE-GUI/OpenLayerMap/
npx vite build
cd dist
python -m http.server 8000      
http://localhost:8000/index.html


python -m http.server           (static)

npm run build
npm start


dead ideas: worker thread for background timer [QTimer can only work in main thread. rip]
other ways: optimise map display (tiles extent)